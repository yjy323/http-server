#ifndef UTILS_HPP
#define UTILS_HPP

#include <string>
#include <vector>

const std::vector<const std::string> split(const std::string& str,
                                           const std::string& sep);

#endif
