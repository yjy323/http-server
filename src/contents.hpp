#ifndef CONTENTS_HPP
#define CONTENTS_HPP

#include <fstream>
#include <iostream>
#include <sstream>
#include <string>

std::string GetMimeType(std::string extension);
std::string GetContents(std::string path);

#endif
